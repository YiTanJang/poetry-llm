---
type: Design
title: 학습 데이터 생성 포맷
description: 시 데이터를 다양한 형태로 변환하여 풍부한 학습 신호를 만드는 전략.
tags: [preprocessing, training-data, formats, data-augmentation]
timestamp: 2026-06-27T00:00:00Z
---

# 학습 데이터 생성 포맷

## 기본 원칙

동일한 시 텍스트를 **여러 관점의 학습 데이터**로 변환하면 모델이 시를 다각도로 깊이 이해할 수 있습니다. 단순히 시적 텍스트를 암기하는 것을 넘어, 시의 **구조, 의도, 미학적 효과, 그리고 퇴고 과정**을 종합적으로 훈련합니다. 각 포맷은 모델이 창작자(시인), 평론가, 편집자의 다중 역할을 습득할 수 있도록 유기적으로 연계되어 설계되었습니다.

---

## CPT vs SFT 토큰화 및 손실 마스킹 전략

시의 예술적 구조를 보존하고 언어 모델에 이식하기 위해, 학습 단계별로 서로 다른 토큰화 설계와 손실(Loss) 계산 마스킹 방식을 사용합니다.

```
+---------------------------------------------------------------------------------+
| 1. Continued Pre-Training (CPT) - 도메인 적응 단계                               |
|   - 학습 대상: 시 코퍼스 전체 (원문 및 특수 구조 토큰)                            |
|   - 손실 계산: 전체 토큰 계산 (Label Masking 없음, labels = input_ids)          |
|   - 구조: [시작] -> 시 텍스트 + <행갈이> + <연갈이> -> [끝] (고정 크기 Packing)     |
+---------------------------------------------------------------------------------+
                                         ↓
+---------------------------------------------------------------------------------+
| 2. Supervised Fine-Tuning (SFT) - 지시어 및 의도 정렬 단계                        |
|   - 학습 대상: 지시 프롬프트, 창작 노트(CoT), 피드백 텍스트, 최종 완성시        |
|   - 손실 계산: 프롬프트 영역 -100 마스킹 (Loss 제외), 타겟 영역에만 손실 가중치 적용 |
|   - 구조: <|im_start|>user ... <|im_end|><|im_start|>assistant (창작노트/수정본)    |
+---------------------------------------------------------------------------------+
```

### 1. CPT (Continual Pre-Training) 토큰화 및 마스킹
* **목적**: 베이스 모델이 한국 현대시의 독창적인 문체, 비유법 및 구조적 토큰을 자연스럽게 내재화하도록 유도합니다.
* **토큰 패킹(Packing)**: 시의 길이가 모델 컨텍스트 길이보다 현저히 짧으므로, 개별 시들을 `<시작>`과 `<끝>` 토큰으로 감싼 뒤, 고정 크기(예: 4,096 토큰)로 이어 붙여 패킹합니다.
* **손실 마스킹**: **마스킹 없음 (labels = input_ids)**. 다음 토큰을 예측하는 Causal Language Modeling(CLM) 손실을 전체 토큰 시퀀스에 걸쳐 100% 계산합니다.

### 2. SFT (Supervised Fine-Tuning) 토큰화 및 마스킹
* **목적**: 메타 정보 정렬, 창작 노트(CoT) 연계 생성, 수정 과정 및 다중 제약조건 하에서의 통제된 생성을 학습합니다.
* **프롬프트 마스킹 (Prompt Masking)**: 모델이 입력 데이터(사용자의 요구사항, 초고, 정렬 템플릿 등)를 그대로 복사하여 손실 값을 줄이는 편향을 방지하기 위해, 입력으로 주어지는 질문 및 제약 조건 영역은 labels 값을 `-100`으로 덮어써서 손실 계산에서 제외합니다.
* **가중 손실 학습 (Weighted Loss)**: CoT 포맷(포맷 3) 등에서는 모델이 중간 추론 과정(창작 노트)과 최종 생성 결과물(시 본문)의 가치가 서로 다름을 학습하도록, 창작 노트 토큰의 Loss 가중치는 `0.5`, 시 본문 토큰의 Loss 가중치는 `1.0`으로 차등 적용합니다.

---

## 7대 학습 데이터 포맷 세부 사항

<a id="포맷-1---완성시-기본"></a>
### 포맷 1 — 완성시 (기본 / Raw Text)

#### 개요 및 학습 목적
모델의 기본적인 시상 전개, 행/연 구분, 운율적 발음 처리를 습득하기 위한 가장 기초적인 포맷입니다. 텍스트 내부에 삽입된 특수 토큰(`<시작>`, `<행갈이>`, `<연갈이>`, `<끝>`)과 발음 정보 레이어(음절수 `<음절:N>`, 연음 현상 `<연음:표기>`)를 함께 학습하여, 다음 토큰 예측 과정에서 자연스럽게 시적 호흡과 소리(음악성)를 배치하는 능력을 기릅니다.

#### 데이터 스키마 (JSON Schema)
```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "RawTextPoemSchema",
  "type": "object",
  "properties": {
    "text": {
      "type": "string",
      "description": "특수 토큰 및 발음 정보 레이어가 내재된 완성시 원문"
    }
  },
  "required": ["text"]
}
```

#### JSON-L 데이터 예시
* **Pretty-printed JSON**:
```json
{
  "text": "<시작>죽는 날까지 하늘을 우러러<음절:14><행갈이>한 점 부끄럼이 없기를<연음:업끼를><음절:10><행갈이>잎새에 이는 바람에도<음절:11><행갈이>나는 괴로워했다<음절:8><연갈이>별을 노래하는 마음으로<음절:11><행갈이>모든 죽어가는 것을 사랑해야지<음절:16><행갈이>그리고 나한테 주어진 길을<음절:13><행갈이>걸어가야겠다<음절:8><연갈이>오늘 밤에도 별이 바람에 스치운다<음절:17><끝>"
}
```
* **JSON-L 직렬화 (단일 라인)**:
```json
{"text": "<시작>죽는 날까지 하늘을 우러러<음절:14><행갈이>한 점 부끄럼이 없기를<연음:업끼를><음절:10><행갈이>잎새에 이는 바람에도<음절:11><행갈이>나는 괴로워했다<음절:8><연갈이>별을 노래하는 마음으로<음절:11><행갈이>모든 죽어가는 것을 사랑해야지<음절:16><행갈이>그리고 나한테 주어진 길을<음절:13><행갈이>걸어가야겠다<음절:8><연갈이>오늘 밤에도 별이 바람에 스치운다<음절:17><끝>"}
```

#### 토큰화 및 손실 마스킹 전략
* **CPT 단계**: 별도의 마스킹 없이 `<시작>`부터 `<끝>`까지 전체 텍스트 토큰에 대해 CLM Loss를 계산합니다.
* **SFT Stage 1 단계**: 기본 형식 훈련을 위해 `text` 전체에 대해 Loss를 계산하며, 패딩 토큰만 `-100` 마스킹을 수행합니다.

---

<a id="포맷-2---시--시론-페어"></a>
### 포맷 2 — 시 + 시론 및 연/행 구성 정렬 (Stanza Alignment)

#### 개요 및 학습 목적
문학적 비평, 수사적 은유 기법 등의 고차원 메타 개념과 시적 언어의 결합도를 높이고, 시의 물리적인 구성 형태(연의 개수, 각 행의 음절수 제한 등)를 정렬 데이터로 반영하여 학습합니다. 모델이 단순히 감정적 발화를 이어가는 것보다 구조적 제약 속에서 형식을 다듬는 법을 학습하게 만듭니다.

#### 데이터 스키마 (JSON Schema)
```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "StanzaAlignmentSchema",
  "type": "object",
  "properties": {
    "metadata": {
      "type": "object",
      "properties": {
        "title": { "type": "string" },
        "author": { "type": "string" },
        "stanza_count": { "type": "integer" }
      },
      "required": ["title", "author", "stanza_count"]
    },
    "alignment": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "stanza_index": { "type": "integer" },
          "theme": { "type": "string" },
          "constraints": {
            "type": "object",
            "properties": {
              "line_count": { "type": "integer" },
              "syllables_per_line": {
                "type": "array",
                "items": { "type": "integer" }
              }
            },
            "required": ["line_count", "syllables_per_line"]
          }
        },
        "required": ["stanza_index", "theme", "constraints"]
      }
    },
    "poem": {
      "type": "string",
      "description": "구조적 특수 토큰이 포함된 시 원문"
    },
    "analysis": {
      "type": "string",
      "description": "해당 시의 주제의식, 수사적 특징 및 연/행 정렬 효과에 관한 미학 분석 평론"
    }
  },
  "required": ["metadata", "alignment", "poem", "analysis"]
}
```

#### JSON-L 데이터 예시
* **Pretty-printed JSON**:
```json
{
  "metadata": {
    "title": "서시",
    "author": "윤동주",
    "stanza_count": 3
  },
  "alignment": [
    {
      "stanza_index": 1,
      "theme": "과거의 고뇌와 실존적 자아 성찰",
      "constraints": {
        "line_count": 4,
        "syllables_per_line": [14, 10, 11, 8]
      }
    },
    {
      "stanza_index": 2,
      "theme": "미래를 향한 소명과 보편적 사랑",
      "constraints": {
        "line_count": 3,
        "syllables_per_line": [11, 16, 13]
      }
    },
    {
      "stanza_index": 3,
      "theme": "현재의 운명 수용과 극복 의지",
      "constraints": {
        "line_count": 2,
        "syllables_per_line": [8, 17]
      }
    }
  ],
  "poem": "<시작>죽는 날까지 하늘을 우러러<행갈이>한 점 부끄럼이 없기를<행갈이>잎새에 이는 바람에도<행갈이>나는 괴로워했다<연갈이>별을 노래하는 마음으로<행갈이>모든 죽어가는 것을 사랑해야지<행갈이>그리고 나한테 주어진 길을<행갈이>걸어가야겠다<연갈이>오늘 밤에도 별이 바람에 스치운다<끝>",
  "analysis": "이 시는 식민지 지식인의 치열한 자아 성찰과 순결성의 의지를 '하늘', '바람', '별'의 대비적 구조 속에서 연과 행의 세밀한 호흡으로 이끌어냅니다. 특히 1연의 촉각적 고뇌에서 2연의 선언을 거쳐 3연의 담담한 운명 극복으로 긴장이 해소되는 3단 정렬 구조는 윤동주 시의 가장 대표적인 형식미를 구현합니다."
}
```
* **JSON-L 직렬화 (단일 라인)**:
```json
{"metadata": {"title": "서시", "author": "윤동주", "stanza_count": 3}, "alignment": [{"stanza_index": 1, "theme": "과거의 고뇌와 실존적 자아 성찰", "constraints": {"line_count": 4, "syllables_per_line": [14, 10, 11, 8]}}, {"stanza_index": 2, "theme": "미래를 향한 소명과 보편적 사랑", "constraints": {"line_count": 3, "syllables_per_line": [11, 16, 13]}}, {"stanza_index": 3, "theme": "현재의 운명 수용과 극복 의지", "constraints": {"line_count": 2, "syllables_per_line": [8, 17]}}], "poem": "<시작>죽는 날까지 하늘을 우러러<행갈이>한 점 부끄럼이 없기를<행갈이>잎새에 이는 바람에도<행갈이>나는 괴로워했다<연갈이>별을 노래하는 마음으로<행갈이>모든 죽어가는 것을 사랑해야지<행갈이>그리고 나한테 주어진 길을<행갈이>걸어가야겠다<연갈이>오늘 밤에도 별이 바람에 스치운다<끝>", "analysis": "이 시는 식민지 지식인의 치열한 자아 성찰과 순결성의 의지를 '하늘', '바람', '별'의 대비적 구조 속에서 연과 행의 세밀한 호흡으로 이끌어냅니다. 특히 1연의 촉각적 고뇌에서 2연의 선언을 거쳐 3연의 담담한 운명 극복으로 긴장이 해소되는 3단 정렬 구조는 윤동주 시의 가장 대표적인 형식미를 구현합니다."}
```

#### 토큰화 및 손실 마스킹 전략
* **SFT Stage 2 단계**:
  * 입력부(`metadata`, `alignment` 리스트, `analysis` 평론 부분) 토큰은 모두 **`-100`으로 마스킹**하여 학습 손실을 발생시키지 않습니다.
  * 최종 목표물인 `poem` (시 본문)의 토큰 영역에만 Loss 가중치 1.0을 적용하여, 구조에 정확히 대응하여 압축적인 시를 생성하도록 모델을 통제합니다.

---

<a id="포맷-3---창작-노트--시-cot-파이프라인의-핵심"></a>
### 포맷 3 — 창작 노트 → 시 (CoT 파이프라인의 핵심)

#### 개요 및 학습 목적
본 프로젝트의 핵심 생성 아키텍처인 Chain of Thought(생각의 사슬)를 모델 내부에 이식하기 위한 포맷입니다. 시를 쓰기 전에 작가의 눈으로 창작 의도, 묘사하고자 하는 감정, 전반적인 시상의 구조화 경로, 이미지 배치 전략을 산문 형태로 서술하고 이를 거쳐 최종 시를 완성하게 유도함으로써, 시적 일관성과 미학적 정교함을 극대화합니다.

#### 데이터 스키마 (JSON Schema)
```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "CreativeNoteToPoemSchema",
  "type": "object",
  "properties": {
    "creative_note": {
      "type": "string",
      "description": "시의 창작 배경, 감정 상태, 비유 및 묘사 전략, 행갈이 배치를 포함한 시상 설계도"
    },
    "poem": {
      "type": "string",
      "description": "창작 노트의 미학 설계 의도를 충실하게 구체화한 최종 시"
    }
  },
  "required": ["creative_note", "poem"]
}
```

#### JSON-L 데이터 예시
* **Pretty-printed JSON**:
```json
{
  "creative_note": "비 내리는 늦은 저녁, 홀로 방 안에 앉아 전등 불빛 아래에서 찾아온 소외감과 내적 고독을 표현하고자 합니다. 창밖에서 들이치는 빗소리가 내 안의 침묵을 더욱 고조시키는 상황을 시각적, 촉각적으로 형상화할 것입니다. 1연에서는 유리창을 두드리는 빗방울과 방 안의 고요한 침묵을 묘사하고, 2연에서는 주황빛 불빛 아래 찻잔을 쥔 시적 화자의 시각적 구도를 부각하겠습니다. 3연에서는 결국 빗소리와 나의 감정이 안팎의 구분 없이 하나로 스며드는 내적 성찰을 담을 것입니다. 호흡의 지연을 주기 위해 명사형이나 조사의 뒤에서 의도적인 행갈이를 수행하도록 유도하겠습니다.",
  "poem": "<시작>창문 틈새로 스며드는 밤의<행갈이>가느다란 빗소리가 귓가를 적실 때<행갈이>방 안은 이미 침묵의 호수가 된다<연갈이>주황색 전등 아래 홀로 쥐어본<행갈이>따뜻한 찻잔의 온기가<행갈이>차가운 손끝을 타고 번져가고<연갈이>창밖의 빗소리는 더는 밖의 것이 아닌<행갈이>내 안에서 소리 없이 흐르는<행갈이>슬픔의 또 다른 이름임을 안다<끝>"
}
```
* **JSON-L 직렬화 (단일 라인)**:
```json
{"creative_note": "비 내리는 늦은 저녁, 홀로 방 안에 앉아 전등 불빛 아래에서 찾아온 소외감과 내적 고독을 표현하고자 합니다. 창밖에서 들이치는 빗소리가 내 안의 침묵을 더욱 고조시키는 상황을 시각적, 촉각적으로 형상화할 것입니다. 1연에서는 유리창을 두드리는 빗방울과 방 안의 고요한 침묵을 묘사하고, 2연에서는 주황빛 불빛 아래 찻잔을 쥔 시적 화자의 시각적 구도를 부각하겠습니다. 3연에서는 결국 빗소리와 나의 감정이 안팎의 구분 없이 하나로 스며드는 내적 성찰을 담을 것입니다. 호흡의 지연을 주기 위해 명사형이나 조사의 뒤에서 의도적인 행갈이를 수행하도록 유도하겠습니다.", "poem": "<시작>창문 틈새로 스며드는 밤의<행갈이>가느다란 빗소리가 귓가를 적실 때<행갈이>방 안은 이미 침묵의 호수가 된다<연갈이>주황색 전등 아래 홀로 쥐어본<행갈이>따뜻한 찻잔의 온기가<행갈이>차가운 손끝을 타고 번져가고<연갈이>창밖의 빗소리는 더는 밖의 것이 아닌<행갈이>내 안에서 소리 없이 흐르는<행갈이>슬픔의 또 다른 이름임을 안다<끝>"}
```

#### Playbook 및 토큰화/손실 마스킹 전략
* **SFT Stage 3 단계**:
  * 창작 요청 지시 프롬프트(예: "아래 조건으로 시 창작 노트를 쓰고 시를 작성하세요")는 **`-100` 마스킹**.
  * `creative_note` (창작 노트) 영역은 추론 경로의 논리 학습을 위해 Loss 가중치 `0.5`로 계산을 진행합니다.
  * `poem` (최종 생성 시) 영역은 가장 지배적인 목적에 부합하도록 Loss 가중치 `1.0`을 유지합니다.

---

<a id="포맷-4---수정-과정-데이터"></a>
### 포맷 4 — 수정 과정 데이터 (Revision)

#### 개요 및 학습 목적
시의 초기 구상(초고)에서 감정 과잉, 상투적 비유, 혹은 리듬의 어색함을 외부 피드백(Critique)에 기반하여 정밀하게 교정하고 정제된 최종본으로 재창조하는 반복 퇴고(Revision Loop) 능력을 이식합니다. 인간 시인과 협업하거나, 모델 스스로 생성물을 피드백에 수렴하여 높은 격조의 시로 개작하는 성능을 극대화합니다.

#### 데이터 스키마 (JSON Schema)
```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "PoemRevisionSchema",
  "type": "object",
  "properties": {
    "draft": {
      "type": "string",
      "description": "감정의 날것 표현이나 상투적 표현이 포함된 시의 초기 초고"
    },
    "critique": {
      "type": "string",
      "description": "초안의 비유적 한계 및 형식적 리듬을 지적하고 대안 방향을 서술한 비평 피드백"
    },
    "revised_poem": {
      "type": "string",
      "description": "비평 지침을 정확하게 수용하여 개선해 낸 최종 수정본 시"
    }
  },
  "required": ["draft", "critique", "revised_poem"]
}
```

#### JSON-L 데이터 예시
* **Pretty-printed JSON**:
```json
{
  "draft": "<시작>차가운 겨울바람이 세차게 분다<행갈이>길가에 서서 눈을 맞으니 눈물이 흐른다<행갈이>너무 슬퍼서 마음이 찢어지는 것 같다<끝>",
  "critique": "'겨울바람', '눈물', '마음이 찢어진다'와 같은 통속적이고 직설적인 정서 서술은 독자의 미학적 거리감을 무너뜨립니다. 바람의 물리적인 날카로운 감촉을 시각화하고, 눈물의 고임이나 번짐을 겨울 사물의 결빙이나 풍경의 침묵 속에 숨겨 정서를 입체적이고 세밀하게 간접 묘사하도록 시를 다시 고쳐 쓰십시오.",
  "revised_poem": "<시작>은빛 칼날 같은 바람이 볼을 스치고<행갈이>얼어붙은 가로등 아래 멈춰 선다<행갈이>굳은 주머니 속 웅크린 손끝으로<행갈이>말을 잃어버린 겨울이 고여온다<끝>"
}
```
* **JSON-L 직렬화 (단일 라인)**:
```json
{"draft": "<시작>차가운 겨울바람이 세차게 분다<행갈이>길가에 서서 눈을 맞으니 눈물이 흐른다<행갈이>너무 슬퍼서 마음이 찢어지는 것 같다<끝>", "critique": "'겨울바람', '눈물', '마음이 찢어진다'와 같은 통속적이고 직설적인 정서 서술은 독자의 미학적 거리감을 무너뜨립니다. 바람의 물리적인 날카로운 감촉을 시각화하고, 눈물의 고임이나 번짐을 겨울 사물의 결빙이나 풍경의 침묵 속에 숨겨 정서를 입체적이고 세밀하게 간접 묘사하도록 시를 다시 고쳐 쓰십시오.", "revised_poem": "<시작>은빛 칼날 같은 바람이 볼을 스치고<행갈이>얼어붙은 가로등 아래 멈춰 선다<행갈이>굳은 주머니 속 웅크린 손끝으로<행갈이>말을 잃어버린 겨울이 고여온다<끝>"}
```

#### 토큰화 및 손실 마스킹 전략
* **SFT Stage 4 단계**:
  * 입력인 `draft` (초고)와 `critique` (비평 피드백) 토큰들은 전부 **`-100`으로 마스킹**합니다.
  * 오직 비평을 수렴해 낸 `revised_poem` (수정본) 영역의 토큰에만 Loss를 100% 반영하여, 문장 구조와 비유를 재구성하는 능력을 극대화하여 연마합니다.

---

<a id="포맷-5---시--설명-역방향"></a>
### 포맷 5 — 시 → 설명 (역방향 / Reverse Commentary)

#### 개요 및 학습 목적
완성된 시 텍스트를 입력받아 그 안에 내재된 미학적 상징성, 수사적 의도, 행갈이와 연갈이가 시의 템포 및 운율에 미치는 예술적 효과를 해설 및 평론하게 하는 포맷입니다. 시적 구조를 읽고 평가하는 능력을 기름으로써, 향후 자율 미학적 평가기(Self-Critique Model) 및 보상 생성 단계의 기초 데이터로 기여합니다.

#### 데이터 스키마 (JSON Schema)
```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ReverseCommentarySchema",
  "type": "object",
  "properties": {
    "poem": {
      "type": "string",
      "description": "분석 및 평론 해설의 대상이 되는 시 본문"
    },
    "commentary": {
      "type": "string",
      "description": "시의 상징과 비유 구조, 율격과 호흡에 관한 입체적 문학 평론 해설"
    }
  },
  "required": ["poem", "commentary"]
}
```

#### JSON-L 데이터 예시
* **Pretty-printed JSON**:
```json
{
  "poem": "<시작>흔들리는 것은<행갈이>바람이 아니다<행갈이>내 마음이다<끝>",
  "commentary": "이 시는 극도의 형식적 긴축을 통해 자아의 실존적 자각을 단 3행으로 집약해냅니다. 1행 '흔들리는 것은' 직후에 단행된 의도적 행갈이는 독자에게 예측을 유예시키는 미시적 호흡의 긴장감을 만듭니다. 2행의 '바람이 아니다'는 흔히 동요의 외적 원인으로 지목되는 현상을 기각하며, 3행 '내 마음이다'에서 비로소 흔들림의 진원지가 외적 자연(바람)이 아닌 시적 자아의 내면 공간(마음)임을 선언하며 주객전도의 완결된 인식을 성취합니다."
}
```
* **JSON-L 직렬화 (단일 라인)**:
```json
{"poem": "<시작>흔들리는 것은<행갈이>바람이 아니다<행갈이>내 마음이다<끝>", "commentary": "이 시는 극도의 형식적 긴축을 통해 자아의 실존적 자각을 단 3행으로 집약해냅니다. 1행 '흔들리는 것은' 직후에 단행된 의도적 행갈이는 독자에게 예측을 유예시키는 미시적 호흡의 긴장감을 만듭니다. 2행의 '바람이 아니다'는 흔히 동요의 외적 원인으로 지목되는 현상을 기각하며, 3행 '내 마음이다'에서 비로소 흔들림의 진원지가 외적 자연(바람)이 아닌 시적 자아의 내면 공간(마음)임을 선언하며 주객전도의 완결된 인식을 성취합니다."}
```

#### 토큰화 및 손실 마스킹 전략
* **SFT Stage 2 단계**:
  * 입력 텍스트인 `poem` 부분은 **`-100`으로 마스킹**합니다.
  * 출력 목표인 `commentary` (평론 해설) 영역에 한해서만 1.0 가중치로 손실 함수를 계산하여 해설 작성을 완성합니다.

---

<a id="포맷-6---빈칸-채우기-구조-학습"></a>
### 포맷 6 — 빈칸 채우기 (구조 학습 / Structure Mask-Fill)

#### 개요 및 학습 목적
시가 갖는 특유의 시각적 형태 및 문체적 리듬감의 뼈대를 주입합니다. 시의 핵심적인 단어나 시각적 경계 토큰인 `<행갈이>`, `<연갈이>` 등을 빈칸(`[MASK]`)으로 가린 뒤 이를 추론하게 유도하여, 모델이 텍스트의 표면적인 글자 전개를 넘어 시의 텍스트가 호흡 속에서 어떻게 시각적·청각적으로 정지되고 맞물리는지 구조적 문법을 입체적으로 이해하도록 돕습니다.

#### 데이터 스키마 (JSON Schema)
```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "StructureMaskFillSchema",
  "type": "object",
  "properties": {
    "masked_poem": {
      "type": "string",
      "description": "구조 토큰이나 중심 시어가 [MASK]로 치환된 불완전한 시 텍스트"
    },
    "targets": {
      "type": "array",
      "description": "[MASK] 위치에 대입될 올바른 형태소 및 특수 토큰의 목록",
      "items": {
        "type": "object",
        "properties": {
          "mask_index": {
            "type": "integer",
            "description": "masked_poem 내에서 출현하는 0-indexed 기반 MASK 순서"
          },
          "correct_token": {
            "type": "string",
            "description": "해당 MASK에 채워질 본래의 특수 토큰 혹은 단어"
          }
        },
        "required": ["mask_index", "correct_token"]
      }
    }
  },
  "required": ["masked_poem", "targets"]
}
```

#### JSON-L 데이터 예시
* **Pretty-printed JSON**:
```json
{
  "masked_poem": "<시작>눈이 오는가 북쪽엔[MASK]함박눈 쏟아져 내리는가[MASK]험한 벌판을 달리는[MASK]울 오빠는 맘 놓고 있느냐<끝>",
  "targets": [
    { "mask_index": 0, "correct_token": "<행갈이>" },
    { "mask_index": 1, "correct_token": "<연갈이>" },
    { "mask_index": 2, "correct_token": "<행갈이>" }
  ]
}
```
* **JSON-L 직렬화 (단일 라인)**:
```json
{"masked_poem": "<시작>눈이 오는가 북쪽엔[MASK]함박눈 쏟아져 내리는가[MASK]험한 벌판을 달리는[MASK]울 오빠는 맘 놓고 있느냐<끝>", "targets": [{"mask_index": 0, "correct_token": "<행갈이>"}, {"mask_index": 1, "correct_token": "<연갈이>"}, {"mask_index": 2, "correct_token": "<행갈이>"}]}
```

#### 토큰화 및 손실 마스킹 전략
* **SFT Stage 1 단계**:
  * 입력 `masked_poem`에 대해서는 **`-100`으로 마스킹**을 적용합니다.
  * 정답 배열 `targets` 내부의 각 `correct_token` 값들에 한해서만 Loss를 계산하여, 시적 공간의 절단점과 쉼표를 포착하는 구조 직관력을 가집니다.

---

<a id="포맷-7---소재--시-창작-요청"></a>
### 포맷 7 — 소재 및 독창성 제약 → 시 (Controlled Generation & Novelty)

#### 개요 및 학습 목적
시적 통제력(Instruction Following)과 함께, 본 연구 프로젝트의 지향점인 **독창성(Novelty)**을 모델에 동적으로 정렬시키기 위한 핵심 튜닝 포맷입니다. 제시된 소재, 필수 단어, 감정의 톤, 정형식(자유시, 시조 등) 제약 조건을 지키면서도, 기존 문학 코퍼스에서의 발생 빈도가 낮거나 의미론적으로 서로 이질적인 임베딩 거리를 나타내는 `target_novelty_score` 수치에 부응하여, 참신하고 전위적인 수사와 소재 결합(OOD, Out-Of-Distribution)을 시도하도록 유도합니다.

#### 데이터 스키마 (JSON Schema)
```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ControlledGenerationNoveltySchema",
  "type": "object",
  "properties": {
    "prompt": {
      "type": "string",
      "description": "시의 테마 및 상상력의 기반이 되는 중심 대상"
    },
    "constraints": {
      "type": "object",
      "properties": {
        "keywords": {
          "type": "array",
          "items": { "type": "string" },
          "description": "시에 의무적으로 출현시켜야 할 단어들"
        },
        "tone": { "type": "string", "description": "시상 전반을 아우르는 핵심 감정의 톤" },
        "form": { "type": "string", "description": "형식 제약 조건 (예: 자유시, 시조, 산문시)" },
        "target_novelty_score": {
          "type": "number",
          "minimum": 0.0,
          "maximum": 1.0,
          "description": "코퍼스 n-gram 빈도 및 어휘 임베딩 유사성을 기반으로 설정한 낯설게 하기(Novelty) 강도 수치"
        }
      },
      "required": ["keywords", "tone", "form", "target_novelty_score"]
    },
    "poem": {
      "type": "string",
      "description": "모든 복합 제약조건과 참신성 수치 목표를 충실하게 달성하여 창조된 시 본문"
    }
  },
  "required": ["prompt", "constraints", "poem"]
}
```

#### JSON-L 데이터 예시
* **Pretty-printed JSON**:
```json
{
  "prompt": "지하철 환승 통로에서 지나가는 군중을 응시하며 느낀 기계주의적 소외감과 시적 정서를 결합하여 시를 써주세요.",
  "constraints": {
    "keywords": ["환승역", "어깨", "지하"],
    "tone": "쓸쓸하고 기계적인 차가움",
    "form": "자유시",
    "target_novelty_score": 0.89
  },
  "poem": "<시작>지하 3층의 복도를 채우는 무수한 어깨의 스침들<행갈이>점멸하는 유기체의 행렬 속에서<행갈이>환승역의 레일은 조용한 가열(加熱)을 거듭하고<연갈이>녹아내린 기계적 호흡 사이로<행갈이>스텐 유리의 단면처럼 빛나는 너의 시선<행갈이>우리들의 침묵은 납으로 주조된 침전물이 된다<끝>"
}
```
* **JSON-L 직렬화 (단일 라인)**:
```json
{"prompt": "지하철 환승 통로에서 지나가는 군중을 응시하며 느낀 기계주의적 소외감과 시적 정서를 결합하여 시를 써주세요.", "constraints": {"keywords": ["환승역", "어깨", "지하"], "tone": "쓸쓸하고 기계적인 차가움", "form": "자유시", "target_novelty_score": 0.89}, "poem": "<시작>지하 3층의 복도를 채우는 무수한 어깨의 스침들<행갈이>점멸하는 유기체의 행렬 속에서<행갈이>환승역의 레일은 조용한 가열(加熱)을 거듭하고<연갈이>녹아내린 기계적 호흡 사이로<행갈이>스텐 유리의 단면처럼 빛나는 너의 시선<행갈이>우리들의 침묵은 납으로 주조된 침전물이 된다<끝>"}
```

#### 토큰화 및 손실 마스킹 전략
* **SFT Stage 3 단계**:
  * 입력 `prompt` 및 `constraints` 메타데이터(목표 독창성 점수 포함) 영역은 전부 **`-100`으로 마스킹**합니다.
  * 출력 `poem`에 대해서만 Loss를 100% 계산하여, 시의 형식 조건들을 모두 고루 충족하면서도 `target_novelty_score`에 지정된 독창적인 어휘 선택 및 참신한 의미론적 도약(예: '납으로 주조된 침묵', '안개의 도금')을 시도하도록 정렬합니다.

---

## 데이터 믹스 비율 (초안)

> 탐색중

SFT 단계의 최종 최적화를 위해 7개 포맷을 다음과 같은 비율로 혼합하여 커리큘럼에 따라 적용합니다.

| 포맷 | 비중 | 비고 |
|:---|:---|:---|
| **포맷 1 (Raw Text - 완성시)** | 30% | 기본 현대시 언어 패턴, 행/연 배치, 소리(발음) 레이어 학습 |
| **포맷 2 (Stanza Alignment - 구성 정렬)** | 20% | 각 연/행 단위 구조 정렬 및 고차원 평론 분석 연계 |
| **포맷 3 (CoT - 창작노트→시)** | 20% | CoT 파이프라인 정착을 위한 단계적 시상 구상 및 논리 훈련 |
| **포맷 4 (Revision - 수정 과정)** | 15% | 비평적 피드백을 수용하여 상투적인 시를 세련된 언어로 퇴고 |
| **포맷 5 (Reverse Commentary - 시→평론)** | 10% | 자율 평가 모델 역량 이식을 위한 완성시 역분석 학습 |
| **포맷 6 (Structure Mask-Fill - 빈칸 채우기)** | 3% | 특수 토큰의 올바른 배치 경계선 학습 및 시적 여백 감각 이식 |
| **포맷 7 (Controlled Gen & Novelty - 제약/독창성)** | 2% | 다중 제약 준수 및 임베딩 도약을 통한 참신성(Novelty) 제어 |

---

## 미결 사항

- [Ph1] **Stanza Alignment의 동적 자수율 매핑 오차 제어**: 각 연의 줄 수와 줄별 자수율(syllables_per_line) 메타데이터를 프롬프트로 주입했을 때, 모델의 실제 생성물이 해당 자수율을 미세하게 초과하거나 도달하지 못해 발생하는 오차를 최소화할 수 있는 디코딩 시점의 Logit Bias 제어나 특수 페널티 메커니즘을 어떻게 설계할 것인가?
- [Ph1] **창작 노트와 시 본문의 부분 손실 가중치(Partial Loss Weight) 최적비**: SFT Stage 3(CoT)에서 창작 노트의 손실 가중치를 0.5가 아닌 극단적인 비율(예: 0.1 vs 1.0)로 설정했을 때, 모델이 창작 노트를 단지 무의미한 텍스트로 흘려보내지 않으면서도 시 본문의 시적 낯설게 하기(Novelty) 자유도를 훼손하지 않는 수학적 임계비율은 얼마인가?
- [Ph1] **Novelty Score 메타데이터의 Autoregressive 학습 정교화**: 포맷 7의 `target_novelty_score` 지수가 단순 텍스트 숫자로 토큰화(예: "0", ".", "8", "9")될 때의 토큰 분절 문제를 극복하고, 모델 내부의 주의(Attention) 헤드가 참신성 점수와 고차원 어휘 임베딩의 OOD 분포를 강하게 상호 연계하도록 만드는 이산적 링귀스틱 버킷 토큰(예: `<novelty_tier_5>`) 또는 임베딩 보정 기법의 효과는 어떠한가?
